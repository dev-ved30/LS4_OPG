#!/usr/bin/env python
"""Load an ls4_sim database into pandas."""

import sys
from sqlalchemy import create_engine
import pandas as pd


if len(sys.argv) != 2:
    print('Usage: load_ls4_sim /path/to/sim.db')

engine = create_engine(f'sqlite:///{sys.argv[1]}')
df = pd.read_sql('Summary', engine)
